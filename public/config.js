window.STOCKLAB_CONFIG = window.STOCKLAB_CONFIG || {
  // Keep empty for same-origin API (local Node server).
  // For GitHub Pages, set this to your hosted backend, e.g. "https://your-api.example.com".
  apiBase: ""
};
